<Page
    x:Class="Xslt2Game.Boxup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Xslt2Game"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Margin="10" Content="Load..." x:Name="LoadButton" Click="LoadButton_Click"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="10" Text="{Binding Url, Mode=TwoWay}"/>
        <Grid  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Background="White">
        <Viewbox x:Name="Viewbox" Stretch="Uniform">
            <Canvas xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:chain="http://mansoft.nl/chain" x:Name="game" Width="3.2" Height="3.2">
                <Canvas.Resources>
                    <ControlTemplate x:Key="mover">
                        <Polyline Points="0.375,0.375 0.625,0.375 0.625,0.625 0.375,0.625" Fill="black"/>
                    </ControlTemplate>
                    <ControlTemplate x:Key="bigbox">
                        <Polyline Points="0.1,0.1 0.1,0.9 0.9,0.9 0.9,0.1" Stroke="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}" StrokeThickness="0.075">
                            <Polyline.RenderTransform>
                                <RotateTransform Angle="{Binding Rotation, RelativeSource={RelativeSource TemplatedParent}}" CenterX="0.5" CenterY="0.5"/>
                            </Polyline.RenderTransform>
                        </Polyline>
                    </ControlTemplate>
                    <ControlTemplate x:Key="smallbox">
                        <Polyline Points="0.225,0.225 0.225,0.8 0.8,0.8 0.8,0.225" Stroke="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}" StrokeThickness="0.075">
                            <Polyline.RenderTransform>
                                <RotateTransform Angle="{Binding Rotation, RelativeSource={RelativeSource TemplatedParent}}" CenterX="0.5" CenterY="0.5"/>
                            </Polyline.RenderTransform>
                        </Polyline>
                    </ControlTemplate>
                </Canvas.Resources>
            </Canvas>
        </Viewbox>
    </Grid>
        <StackPanel x:Name="StatusPanel" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Vertical">
            <Border x:Name="StatusBorder">
                <!-- Use LiveSetting to enable screen readers to announce the status update. -->
                <TextBlock x:Name="StatusBlock" FontWeight="Bold" AutomationProperties.LiveSetting="Assertive" Foreground="White"/>
            </Border>
        </StackPanel>
    </Grid>
</Page>
